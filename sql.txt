-- 1. 기존에 잘못 만들어진 테이블이나 시퀀스가 있다면 삭제 (처음 만드는 거라면 생략 가능)
DROP SEQUENCE SEQ_RESERVATION;
DROP TABLE RESERVATION;

-- 2. 예약 번호를 자동으로 1씩 증가시켜 줄 시퀀스 생성
CREATE SEQUENCE SEQ_RESERVATION
START WITH 1
INCREMENT BY 1
NOCACHE;

-- 3. 예약 데이터를 담을 실제 테이블 생성
CREATE TABLE RESERVATION (
    RES_NO NUMBER PRIMARY KEY,             -- 예약 고유 번호 (PK)
    EMP_NO NUMBER NOT NULL,                -- 예약자 사원 번호
    ROOM_CODE VARCHAR2(50) NOT NULL,       -- 회의실 코드 (방 번호)
    RES_DATE DATE NOT NULL,                -- 예약 날짜
    START_TIME VARCHAR2(20) NOT NULL,      -- 시작 시간
    END_TIME VARCHAR2(20) NOT NULL,        -- 종료 시간
    PURPOSE VARCHAR2(200),                 -- 사용 목적
    STATUS VARCHAR2(20) DEFAULT '예약완료' -- 예약 상태
);

-- 4. 변경사항을 DB에 완전히 적용
COMMIT;

## 2. DB 생성 후 최종 테스트 진행
테이블과 시퀀스가 성공적으로 만들어졌다면, 다시 이클립스로 돌아와서 다음 순서로 최종 테스트를 해보시면 됩니다.

톰캣 서버를 완전히 껐다가 다시 켭니다.

로그인 화면부터 진입하여 방을 하나 선택하고 예약을 진행합니다.

콘솔 에러 없이 '성공적으로 완료되었습니다' 팝업이 뜨는지 확인합니다.

내 예약 조회 화면에 가서 방금 한 예약이 잘 뜨는지, 그리고 취소 버튼을 눌렀을 때 상태가 '취소됨'으로 잘 변하는지 확인합니다.

## 3. 잊지 말아야 할 안전 저장 (Git Commit)
DB 세팅에 들어가기 전에, 지금까지 완벽하게 완성한 취소 기능과 DB 연동 코드를 깃에 저장해서 날아가지 않도록 박제해 둡니다.

Bash
git add .
git commit -m "2026-04-30: 내 예약 조회(SELECT) 및 예약 취소(UPDATE) DB 연동 완료"
git push origin dev-ghs